{
  "name": "omni-media-ledger",
  "private": true,
  "description": "No build step for the app itself -- this package.json exists only to run the committed regression suite in test/regression.js.",
  "scripts": {
    "test": "node scripts/validate-corpus.js && node test/schema.js && node test/fetch-facts.js && node test/fetch-substance.js && node test/score-batch.js && node test/regression.js",
    "validate-corpus": "node scripts/validate-corpus.js",
    "test-schema": "node test/schema.js",
    "test-facts": "node test/fetch-facts.js && node test/fetch-substance.js",
    "fetch-facts": "node scripts/fetch-facts.js",
    "fetch-substance": "node scripts/fetch-substance.js",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.10.0",
    "globals": "^17.12.0",
    "playwright-core": "^1.63.0"
  }
}
