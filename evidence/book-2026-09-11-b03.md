# Review queue -- book -- 2026-09-11

Everything here needs a human. Grade A proposals are not listed: `scripts/apply-facts.js`
applies those and records them in the JSON beside this file.
**100 fields awaiting a decision.**
**41 additional naming-only fields omitted** -- soft field, sources agree with each other, differ from the corpus only in naming (e.g. "Warner Bros." vs "Warner Bros. Pictures"). Full detail is in the evidence JSON.
**96 additional fields already resolved, no review needed** -- sources disagreed with each other, but the corpus value exactly matches one of them; kept as-is. Full detail is in the evidence JSON.


## Grapes of Wrath (b208)
- **page count** -- corpus has `464`, one source says Google Books `532`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## A Storm of Swords (b210)
- **page count** -- corpus has `973`, one source says Google Books `1009`
- **publisher** -- corpus has `"Bantam"`, sources disagree (edition-dependent): Google Books `"Random House Worlds"`, Wikidata `"AST"`  _(corpus value does not match either source (Google Books "Random House Worlds", Wikidata "AST") -- treated as a distinct edition; the specific cut is not identified from available evidence.)_

## Man's Search for Meaning (b211)
- **page count** -- corpus has `165`, one source says Google Books `168`

## Tinker Tailor Soldier Spy (b212)
- **page count** -- corpus has `355`, one source says Google Books `478`

## To the Lighthouse (b213)
- **page count** -- corpus has `209`, one source says Google Books `197`

## The Waste Land and Other Poems (b214)
- **first publication year** -- corpus has `1922`, one source says OpenLibrary `1934`

## The Complete Poems of Robert Frost (b215)
- **first publication year** -- corpus has `1969`, sources disagree (sources-disagree): OpenLibrary `1951`, Google Books `1971`
- **page count** -- corpus has `607`, one source says Google Books `332`

## The Metamorphosis (b216)
- **page count** -- corpus has `201`, one source says Google Books `42`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## The Republic (b217)
- **first publication year** -- corpus has `-380`, one source says Google Books `2017`  _(corpus year (-380) predates the print era; Google Books 2017 likely describe a modern edition, not the original composition date -- not auto-applied.)_
- **page count** -- corpus has `416`, one source says Google Books `350`

## Team of Rivals (b219)
- **page count** -- corpus has `916`, one source says Google Books `944`

## Small Gods (b220)
- **page count** -- corpus has `320`, one source says Google Books `402`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## The Diary of a Young Girl (b223)
- **first publication year** -- corpus has `1947`, sources disagree (sources-disagree): OpenLibrary `2001`, Google Books `2011`
- **page count** -- corpus has `283`, one source says Google Books `345`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## The Adventures of Sherlock Holmes (b224)
- **page count** -- corpus has `307`, one source says Google Books `238`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## Letters from a Stoic (b225)
- **first publication year** -- corpus has `65`, sources disagree (sources-disagree): OpenLibrary `1969`, Google Books `2016`
- **page count** -- corpus has `254`, one source says Google Books `483`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## Jane Eyre (b226)
- **page count** -- corpus has `500`, one source says Google Books `669`

## Behave (b228)
- **author** -- corpus has `"Robert Sapolsky"`, one source says OpenLibrary `"Robert M. Sapolsky"`

## The Long Goodbye (novel) (b229)
- **page count** -- corpus has `379`, sources disagree (edition-dependent): Google Books `438`, Wikidata `320`  _(corpus value does not match either source (Google Books 438, Wikidata 320) -- treated as a distinct edition; the specific cut is not identified from available evidence.)_
- **publisher** -- corpus has `"Houghton Mifflin"`, sources disagree (edition-dependent): Google Books `"Harper Collins"`, Wikidata `"Hamish Hamilton"`  _(corpus value does not match either source (Google Books "Harper Collins", Wikidata "Hamish Hamilton") -- treated as a distinct edition; the specific cut is not identified from available evidence.)_

## Great Expectations (b230)
- **page count** -- corpus has `544`, one source says Google Books `930`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## The Handmaid's Tale (b231)
- **page count** -- corpus has `311`, one source says Google Books `412`

## Mrs Dalloway (b232)
- **page count** -- corpus has `194`, one source says Google Books `240`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## Silent Spring (b234)
- **page count** -- corpus has `378`, one source says Google Books `406`
- **publisher** -- corpus has `"Houghton Mifflin"`, sources disagree (edition-dependent): Google Books `"Mariner Books"`, Wikidata `"Houghton Mifflin Harcourt"`  _(corpus value does not match either source (Google Books "Mariner Books", Wikidata "Houghton Mifflin Harcourt") -- treated as a distinct edition; the specific cut is not identified from available evidence.)_

## Educated (b235)
- **page count** -- corpus has `334`, one source says Google Books `353`

## One Day in the Life of Ivan Denisovich (b237)
- **page count** -- corpus has `182`, one source says Google Books `177`

## Kindred (b238)
- **page count** -- corpus has `264`, one source says Google Books `292`

## The Man Who Mistook His Wife for a Hat (b239)
- **page count** -- corpus has `243`, one source says Google Books `260`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## Demons (b240)
- **first publication year** -- corpus has `1872`, one source says Wikidata `1871`
- **author** -- corpus has `"Fyodor Dostoevsky"`, one source says Wikidata `"Fyodor Dostoyevsky"`

## Seven Brief Lessons on Physics (b241)
- **page count** -- corpus has `96`, one source says Google Books `63`
- **publisher** -- corpus has `"Adelphi"`, sources disagree (edition-dependent): Google Books `"Penguin UK"`, Wikidata `"Penguin Books"`  _(corpus value does not match either source (Google Books "Penguin UK", Wikidata "Penguin Books") -- treated as a distinct edition; the specific cut is not identified from available evidence.)_

## Entangled Life (b242)
- **page count** -- corpus has `352`, one source says Google Books `370`

## Vagabond: Volume 1 (b244)
- **first publication year** -- corpus has `1998`, one source says Google Books `2015`
- **page count** -- corpus has `320`, one source says Google Books `0`

## Absalom, Absalom! (b245)
- **page count** -- corpus has `320`, one source says Google Books `434`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## The Character of Physical Law (b247)
- **page count** -- corpus has `173`, one source says Google Books `185`

## The Sixth Extinction (b248)
- **page count** -- corpus has `319`, one source says Google Books `337`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## All the Pretty Horses (b250)
- **page count** -- corpus has `302`, one source says Google Books `322`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## The Rise and Fall of the Third Reich (b252)
- **page count** -- corpus has `1245`, one source says Google Books `1268`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## Circe (b253)
- **page count** -- corpus has `393`, one source says Google Books `546`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## As I Lay Dying (b254)
- **page count** -- corpus has `267`, one source says Google Books `183`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## True Grit (novel) (b255)
- **first publication year** -- corpus has `1968`, one source says Google Books `2010`
- **page count** -- corpus has `235`, one source says Google Books `127`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## Midnight's Children (b256)
- **page count** -- corpus has `647`, one source says Google Books `561`

## Love in the Time of Cholera (b257)
- **page count** -- corpus has `348`, sources disagree (edition-dependent): Google Books `458`, Wikidata `464`  _(corpus value does not match either source (Google Books 458, Wikidata 464) -- treated as a distinct edition; the specific cut is not identified from available evidence.)_
- **publisher** -- corpus has `"Editorial Oveja Negra"`, sources disagree (edition-dependent): Google Books `"Everyman's Library"`, Wikidata `"Penguin Random House"`  _(corpus value does not match either source (Google Books "Everyman's Library", Wikidata "Penguin Random House") -- treated as a distinct edition; the specific cut is not identified from available evidence.)_

## The Idiot (b258)
- **page count** -- corpus has `656`, one source says Google Books `692`

## East of Eden (b259)
- **page count** -- corpus has `601`, one source says Google Books `680`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## The Immortal Life of Henrietta Lacks (b261)
- **page count** -- corpus has `381`, one source says Google Books `386`

## Their Eyes Were Watching God (b266)
- **page count** -- corpus has `219`, one source says Google Books `0`
- **publisher** -- corpus has `"Lippincott"`, sources disagree (edition-dependent): Google Books `"Perfection Learning"`, Wikidata `"J. B. Lippincott & Co."`  _(corpus value does not match either source (Google Books "Perfection Learning", Wikidata "J. B. Lippincott & Co.") -- treated as a distinct edition; the specific cut is not identified from available evidence.)_

## The Martian Chronicles (b269)
- **page count** -- corpus has `222`, one source says Google Books `256`

## White Noise (b270)
- **first publication year** -- corpus has `1985`, sources disagree (sources-disagree): OpenLibrary `1984`, Wikidata `1986`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## The Stone Sky (b271)
- **page count** -- corpus has `398`, one source says Google Books `365`

## Into Thin Air (b272)
- **first publication year** -- corpus has `1997`, sources disagree (sources-disagree): OpenLibrary `1996`, Google Books `1998`
- **page count** -- corpus has `378`, one source says Google Books `318`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## The Elegant Universe (b273)
- **page count** -- corpus has `448`, one source says Google Books `447`
- **publisher** -- corpus has `"W.W. Norton"`, sources disagree (edition-dependent): Google Books `"Turtleback"`, Wikidata `"W. W. Norton & Company"`  _(corpus value does not match either source (Google Books "Turtleback", Wikidata "W. W. Norton & Company") -- treated as a distinct edition; the specific cut is not identified from available evidence.)_

## Suttree (b275)
- **page count** -- corpus has `471`, one source says Google Books `482`

## Reality Is Not What It Seems (b276)
- **page count** -- corpus has `280`, one source says Google Books `254`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## On the Shortness of Life (b277)
- **first publication year** -- corpus has `49`, sources disagree (sources-disagree): OpenLibrary `2005`, Google Books `2024`
- **page count** -- corpus has `106`, one source says Google Books `184`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## Fermat's Enigma (b278)
- **page count** -- corpus has `315`, one source says Google Books `352`
- **publisher** -- corpus has `"Walker"`, sources disagree (edition-dependent): Google Books `"Anchor"`, Wikidata `"dtv Verlagsgesellschaft"`  _(corpus value does not match either source (Google Books "Anchor", Wikidata "dtv Verlagsgesellschaft") -- treated as a distinct edition; the specific cut is not identified from available evidence.)_

## Pedro Páramo (b279)
- **page count** -- corpus has `124`, one source says Google Books `180`
- **publisher** -- corpus has `"FCE"`, sources disagree (edition-dependent): Google Books `"University of Texas Press"`, Wikidata `"Fondo de Cultura Económica"`  _(corpus value does not match either source (Google Books "University of Texas Press", Wikidata "Fondo de Cultura Económica") -- treated as a distinct edition; the specific cut is not identified from available evidence.)_

## The Sparrow (b280)
- **page count** -- corpus has `408`, one source says Google Books `515`

## Parable of the Sower (b281)
- **page count** -- corpus has `345`, one source says Google Books `295`

## Beyond Good and Evil (b282)
- **page count** -- corpus has `240`, one source says Google Books `529`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## Rebecca (b283)
- **page count** -- corpus has `449`, one source says Google Books `408`

## The Code Book (b285)
- **page count** -- corpus has `412`, sources disagree (edition-dependent): Google Books `436`, Wikidata `416`  _(corpus value does not match either source (Google Books 436, Wikidata 416) -- treated as a distinct edition; the specific cut is not identified from available evidence.)_
- **publisher** -- corpus has `"Fourth Estate"`, sources disagree (edition-dependent): Google Books `"Vintage"`, Wikidata `"Doubleday"`  _(corpus value does not match either source (Google Books "Vintage", Wikidata "Doubleday") -- treated as a distinct edition; the specific cut is not identified from available evidence.)_

## A Gentleman in Moscow (b286)
- **page count** -- corpus has `462`, one source says Google Books `545`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## The Old Man and the Sea (b287)
- **page count** -- corpus has `127`, one source says Google Books `68`
- **publisher** -- corpus has `"Scribner"`, sources disagree (edition-dependent): Google Books `"Hueber Verlag"`, Wikidata `"Charles Scribner's Sons"`  _(corpus value does not match either source (Google Books "Hueber Verlag", Wikidata "Charles Scribner's Sons") -- treated as a distinct edition; the specific cut is not identified from available evidence.)_

## If on a winter's night a traveler (b288)
- **page count** -- corpus has `260`, one source says Google Books `280`
- **publisher** -- corpus has `"Einaudi"`, sources disagree (edition-dependent): Google Books `"Mariner Books"`, Wikidata `"Giulio Einaudi editions"`  _(corpus value does not match either source (Google Books "Mariner Books", Wikidata "Giulio Einaudi editions") -- treated as a distinct edition; the specific cut is not identified from available evidence.)_

## A Deepness in the Sky (b289)
- **page count** -- corpus has `775`, one source says Google Books `802`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## The Myth of Sisyphus (b291)
- **page count** -- corpus has `212`, one source says Google Books `224`
- **publisher** -- corpus has `"Gallimard"`, sources disagree (edition-dependent): Google Books `"Penguin UK"`, Wikidata `"Hamish Hamilton"`  _(corpus value does not match either source (Google Books "Penguin UK", Wikidata "Hamish Hamilton") -- treated as a distinct edition; the specific cut is not identified from available evidence.)_

## Consider the Lobster (b295)
- **page count** -- corpus has `343`, one source says Google Books `403`

## Wuthering Heights (b296)
- **page count** -- corpus has `416`, one source says Google Books `614`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## The Overstory (b297)
- **page count** -- corpus has `502`, one source says Google Books `531`
- **publisher** -- corpus has `"Norton"`, sources disagree (edition-dependent): Google Books `"Vintage Canada"`, Wikidata `"W. W. Norton & Company"`  _(corpus value does not match either source (Google Books "Vintage Canada", Wikidata "W. W. Norton & Company") -- treated as a distinct edition; the specific cut is not identified from available evidence.)_

## A Clash of Kings (b298)
- **page count** -- corpus has `768`, one source says Google Books `769`

## Nicomachean Ethics (b300)
- **first publication year** -- corpus has `-340`, sources disagree (sources-disagree): OpenLibrary `1558`, Google Books `2011`
- **page count** -- corpus has `329`, one source says Google Books `366`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## Words of Radiance (b301)
- **page count** -- corpus has `1087`, one source says Google Books `1210`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## Discourses and Selected Writings (b302)
- **first publication year** -- corpus has `108`, one source says OpenLibrary `2008`, Google Books `2008`  _(corpus year (108) predates the print era; OpenLibrary 2008, Google Books 2008 likely describe a modern edition, not the original composition date -- not auto-applied.)_
- **page count** -- corpus has `304`, one source says Google Books `365`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## A Supposedly Fun Thing I'll Never Do Again (b304)
- **page count** -- corpus has `353`, one source says Google Books `549`

## A Fine Balance (b305)
- **page count** -- corpus has `603`, one source says Google Books `930`

## The Cyberiad (b306)
- **page count** -- corpus has `295`, one source says Google Books `316`
- _(+1 naming-only field not shown -- sources agree with each other, differ from the corpus only in naming; see the evidence JSON.)_

## Chaos: Making a New Science (b307)
- **page count** -- corpus has `352`, one source says Google Books `386`
- **publisher** -- corpus has `"Viking"`, sources disagree (edition-dependent): Google Books `"Penguin Group"`, Wikidata `"Vintage Books"`  _(corpus value does not match either source (Google Books "Penguin Group", Wikidata "Vintage Books") -- treated as a distinct edition; the specific cut is not identified from available evidence.)_
